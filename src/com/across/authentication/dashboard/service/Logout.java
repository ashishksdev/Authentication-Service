package com.across.authentication.dashboard.service;

public class Logout implements LoginService {
    @Override
    public void execute(){
        // Update session logout log in LoginSessionLog
        
    }
}
